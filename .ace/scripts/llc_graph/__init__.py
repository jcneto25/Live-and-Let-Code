"""llc_graph — modelo de grafo dirigido acíclico (DAG) do pipeline LLC.

ADR-0004 §2.3 — a camada de orquestração do pipeline: estrutura declarada
(REGISTRY + gates.json + dependency-graph.yaml) e estado derivado das
sessões ACE. Este PRP (GRAPH-1A) entrega model.py + builder.py + state.py.
"""
