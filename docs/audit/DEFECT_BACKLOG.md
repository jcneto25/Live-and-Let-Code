# Defect Backlog

| ID | Severity | Category | Location | Intended | Actual | Proof | Fix target | Status |
|----|----------|----------|----------|----------|--------|-------|------------|--------|
| A-01 | CRITICAL | gate-logic (NameError) | `.ace/scripts/llc_wave.py` (orphaned body in `_prp_to_keywords`, call site `run_wave:681`) | `run_wave` calls module-level `_pre_wave_check(dry_run=..., wave_num=...)` which runs `pre-wave-check.sh --build-only` and returns `False` on a failed build so the wave aborts before any PRP session opens. | `_pre_wave_check` was never defined as a function — its intended body was dead code sitting *inside* `_prp_to_keywords` after that function's `return`, with only a stray docstring where the `def` header should be. Name undefined → `run_wave` raises `NameError` at line 681 on every non-dry-run wave. | P2 probe: `has _pre_wave_check: False` / `AttributeError: module 'llc_wave' has no attribute '_pre_wave_check'` (see `docs/audit/p2-enforcement-log.md` #7, `BEHAVIOR_BASELINE.md` #7). | Task 7 (P5 hotfix A) — restore module-level `def _pre_wave_check(...)`, remove dead block from `_prp_to_keywords`. | hotfixed-in-audit |
| A-02 | CRITICAL | gate-logic (no-op returning None) | `.ace/scripts/llc_wave.py` (`_post_wave_check` nested-def, call site `run_wave:752`) | `_post_wave_check` runs `pre-wave-check.sh`, consistency-check, and `prp_verify` after each wave; on a CRITICAL (`prp_verify` exit 2) it returns `False` so `run_wave` blocks the wave (`if not _post_wave_check(...)` at `llc_wave.py:752`), honoring `LLC_PRP_NO_VERIFY`. | `_post_wave_check` declared an outer `def` whose **entire body was a nested `def _post_wave_check(...)`** (never called) — the outer function fell off the end and returned `None`. The intended logic was unreachable dead code inside the nested def. At the call site `if not None == True` → `run_wave` always emitted "pós-onda bloqueada" and returned `False`, making every wave look blocked regardless of the real PRP state (post-wave CRITICAL block inert). | P2 probe: `_post_wave_check returns: None` (see `docs/audit/p2-enforcement-log.md` #6, `BEHAVIOR_BASELINE.md` #6). | Task 8 (P5 hotfix B) — de-nest: add `-> bool` to the outer signature, delete the inner `def` header, de-indent its body into the outer function. | hotfixed-in-audit |
