# Defect Backlog

| ID | Severity | Category | Location | Intended | Actual | Proof | Fix target | Status |
|----|----------|----------|----------|----------|--------|-------|------------|--------|
| A-01 | CRITICAL | gate-logic (NameError) | `.ace/scripts/llc_wave.py` (orphaned body in `_prp_to_keywords`, call site `run_wave:681`) | `run_wave` calls module-level `_pre_wave_check(dry_run=..., wave_num=...)` which runs `pre-wave-check.sh --build-only` and returns `False` on a failed build so the wave aborts before any PRP session opens. | `_pre_wave_check` was never defined as a function — its intended body was dead code sitting *inside* `_prp_to_keywords` after that function's `return`, with only a stray docstring where the `def` header should be. Name undefined → `run_wave` raises `NameError` at line 681 on every non-dry-run wave. | P2 probe: `has _pre_wave_check: False` / `AttributeError: module 'llc_wave' has no attribute '_pre_wave_check'` (see `docs/audit/p2-enforcement-log.md` #7, `BEHAVIOR_BASELINE.md` #7). | Task 7 (P5 hotfix A) — restore module-level `def _pre_wave_check(...)`, remove dead block from `_prp_to_keywords`. | hotfixed-in-audit |
| A-02 | CRITICAL | gate-logic (no-op returning None) | `.ace/scripts/llc_wave.py` (`_post_wave_check` nested-def, call site `run_wave:752`) | `_post_wave_check` runs `pre-wave-check.sh`, consistency-check, and `prp_verify` after each wave; on a CRITICAL (`prp_verify` exit 2) it returns `False` so `run_wave` blocks the wave (`if not _post_wave_check(...)` at `llc_wave.py:752`), honoring `LLC_PRP_NO_VERIFY`. | `_post_wave_check` declared an outer `def` whose **entire body was a nested `def _post_wave_check(...)`** (never called) — the outer function fell off the end and returned `None`. The intended logic was unreachable dead code inside the nested def. At the call site `if not None == True` → `run_wave` always emitted "pós-onda bloqueada" and returned `False`, making every wave look blocked regardless of the real PRP state (post-wave CRITICAL block inert). | P2 probe: `_post_wave_check returns: None` (see `docs/audit/p2-enforcement-log.md` #6, `BEHAVIOR_BASELINE.md` #6). | Task 8 (P5 hotfix B) — de-nest: add `-> bool` to the outer signature, delete the inner `def` header, de-indent its body into the outer function. | hotfixed-in-audit |
| E-13 | MEDIUM | gate-logic (silent-bypass / fail-open) | `.ace/scripts/llc_harness.py:68-94` (`gate_check`), call sites in `llc.py` (`run`, `pipeline`, `wave_run`, `delta_start`) | `gate_check` is the human A/R checkpoint; unknown / ambiguous input must not silently approve — ambiguity should at least surface as a decision (spec anchor #8: auto-approve path is logged/visible, not silent). | **Fail-open.** `choice = input().strip().lower()`; `a`/`approve` → `approved`; `r`/`reject` → `rejected`; **fall-through `return "approved"` (line 94) for ANY other input** — blank line, `x`, `no`, `n`, `EOF-after-empty`, or a typo like `rejct` all approve. Only the exact tokens `r`/`reject` reject. `--auto-approve` (CI) approves without interaction (logged via `⚡` print). No timeout auto-approve. The "human checkpoint" provides no enforcement against an inattentive or non-interactive operator. | P2 #13: `sed -n '68,94p' .ace/scripts/llc_harness.py` shows the fall-through `return "approved"`. Also reached transitively by #14 (zone-red replay) and #11 (test-coverage CLI gate) which both call `gate_check`. (see `docs/audit/p2-enforcement-log.md` #13, `BEHAVIOR_BASELINE.md` #13). | **design-decision** (escalated) — needs a policy call on the default: keep fail-open but make ambiguity explicit (require `a`/`approve` for approve; treat empty/unknown as `pending` or prompt again), OR document fail-open as intentional. Touches every CLI A/R path. | resolved — policy: interactive `gate_check` requires explicit `a`/`approve` to approve; any non-`a`/`approve`/`r`/`reject` input re-prompts (never approves silently). `--auto-approve` (CI) unchanged. |
| E-12 | MEDIUM | gate-logic (smart-skip trusts DELTA_REPORT verbatim) | `.ace/scripts/llc_delta.py:132-143` (`is_step_skipped`), `:153-173` (`generate_skip_note`) | Smart-skip honors `skip_steps` from `DELTA_REPORT.md` but protects "always-executed" steps (guide: 10, 10.6, 10.7, 10.8, 11, 11.1, 11.2 must always run) so a malformed/adversarial delta cannot auto-approve-skip the PRP-acceptance and coverage gates. | **No guardrail.** `is_step_skipped` is driven entirely by the `skip_steps` list — no allow-list validation, no protection of always-executed steps (`grep` for `sempre`/`always`/`always_exec`/`protected` returns nothing). A `DELTA_REPORT.md` listing `step_id: "11.2"` (PRP-acceptance) or `"10.8"` (coverage) returns True and `generate_skip_note` writes `**Gate:** ✅ Auto-aprovado via Smart Skip` — defeating #4/#9/#11. | P2 #12: `sed -n '132,175p' .ace/scripts/llc_delta.py`; `grep -n "sempre\|always\|always_exec\|protected\|10.6\|10.7\|10.8\|11.1\|11.2" .ace/scripts/llc_delta.py` → none (see `docs/audit/p2-enforcement-log.md` #12, `BEHAVIOR_BASELINE.md` #12). | **design-decision** (escalated) — needs a policy decision on which steps are unskippable (the allow-list) before a hotfix can be written. Then: add an `ALWAYS_RUN` set to `llc_delta.py` and have `is_step_skipped` return False for them regardless of `DELTA_REPORT.md`. | resolved — `ALWAYS_RUN = {10,10.6,10.7,10.8,11,11.1,11.2}` added; `is_step_skipped` normalizes step ids (via `llc_steps.canonical_id`) and returns False for always-run steps regardless of `DELTA_REPORT.md`. |
| E-11 | LOW | enforcement (doc/UX hazard) | `.ace/scripts/llc.py:45,88,408-425` (`gate_run`, `test-coverage`/`10.8`/`10-COVERAGE` aliases) | `llc gate run --gate test-coverage` enforces the test-coverage gate. | **Inert (checklist-only).** The gate has three names for one gate (`10-COVERAGE` / `10.8` / `test-coverage`). `gate_run` in `--dry-run` prints `_show_gate_checklist` and returns; otherwise prints "🔍 Validando Gate …" and calls `gate_check` (the fail-open human checkpoint from #13). The command does **not** measure coverage itself; the real coverage threshold lives in `prp_verify.check_project_coverage`, invoked on the CRITICAL path of #9 (routed through #4's merge block), not in this command. | P2 #11: `sed -n '345,425p' .ace/scripts/llc.py`; `grep -n '"test-coverage"\|"10.8"\|check_project_coverage' .ace/scripts/llc.py .ace/scripts/prp_verify.py` (see `docs/audit/p2-enforcement-log.md` #11, `BEHAVIOR_BASELINE.md` #11). | **doc-only** — clarify in the guide/help that `llc gate run --gate test-coverage` is a checklist + human checkpoint, not a meter; point users to `prp_verify` (the CRITICAL-path coverage enforcement) for the actual threshold. Optionally collapse the 3-name alias split. | resolved — `10-coverage`/`10-COVERAGE` agora resolvem para `10.8` via `GATE_ALIASES` (lookup case-insensitive); `gate_run` docstring + help deixam claro que é checklist manual, e o run imprime nota apontando `prp_verify` (step 10.8 / dente #9) como o medidor real de cobertura. |
| D-01 | LOW | conformance-drift (stale display field) | `.ace/config/gates.json` — gates `11-SEC`, `12-NULL`, `11-OWASP` (`step` field) | Each gate's `step` display field equals the `number` of the step whose `gate` resolves to it (per `llc_steps.py` REGISTRY): `11-SEC` should read `10.6` (step "10.6" Security Audit), `12-NULL` should read `10.7` (step "10.7" Null Safety), `11-OWASP` should read `11.1` (step "11.1" OWASP Hardening). | The three gates still carry their pre-renumber `step` values: `11-SEC`→`11`, `12-NULL`→`12`, `11-OWASP`→`11` (left over from the 10.6/10.7/11.1 step renumber done in `llc_steps.py`). The `step`→`gate` resolution itself is correct (every `StepSpec.gate` resolves to a key with a non-empty checklist, no two steps collide); only the human-facing display field is stale. | `python3 .ace/scripts/test_llc_steps.py` characterization test PASSES — it deliberately does NOT assert `gates[gate]["step"] == spec.number`. Direct probe: `11-SEC.step=11`, `12-NULL.step=12`, `11-OWASP.step=11` (all three wrong vs. expected 10.6/10.7/11.1). | doc-only / sub-project-2 — update the three `step` display fields in `gates.json` to match `llc_steps.py` numbers. No enforcement or code-path depends on the display field. | resolved — `gates.json` updated: `11-SEC.step=10.6`, `12-NULL.step=10.7`, `11-OWASP.step=11.1`. Resolution table now shows 0 mismatches. |
| D-02 | INFO | conformance-drift (unwired steps) | `.ace/scripts/llc_steps.py` (`REGISTRY`, `normalize_step`) — steps 5a/5b/5c | Steps 5a (Architecture Patterns), 5b (API Design), 5c (Clean Code) are part of the pipeline and resolvable via `normalize_step`. | Steps 5a/5b/5c are **NOT** in the committed `REGISTRY` — `normalize_step("5.1"/"5b"/"5c")` raises `UnknownStepError`. The skills `llc-step-5b-api-design.md`, `llc-step-5c-clean-code.md`, `llc-step-api-design.md` exist on disk (untracked) but the pipeline wiring is the reverted in-flight work. (The large gate-remap drift that motivated the original Task 9 — missing gates 4/5, key collisions — DOES NOT EXIST at baseline `a519d0f`; gates 4/5 are present and keys align.) | P3: `normalize_step("5.1"/"5b"/"5c")` → `UnknownStepError` (see `docs/audit/p3-conformance-findings.md` D-02, `BEHAVIOR_BASELINE.md` D-02). | out-of-scope — re-wiring 5a/5b/5c is pending the reverted in-flight work; excluded from this baseline's scope. Do NOT apply any gate remap or add gates 4/5. | out-of-scope |
| ADV-03 | INFO | enforcement (advisory by design) | `.ace/scripts/pre-commit.sh:106-122` (`impact-analyzer.py`, `fitness-functions.py` invocations) | Pre-commit impact + fitness output is advisory only — never blocks the commit (spec: #3 is an advisory tooth, not a hard block). | **Working as designed.** `impact-analyzer.py` stderr → `/dev/null`, wrapped in `|| echo`; `fitness-functions.py` piped through a summarizer with stderr `2>/dev/null` then `|| echo`. A textual `🔴 BLOQUEIO` is never tested and never increments `ERRORS`. Neither call touches the `ERRORS` counter. | P2 #3: `grep -n "impact-analyzer\|fitness-functions\|2>/dev/null\|ERRORS=" .ace/scripts/pre-commit.sh` (see `docs/audit/p2-enforcement-log.md` #3). | none — working as designed; advisory surfacing only. | info (not a defect) |
| ADV-15 | INFO | enforcement (advisory by design) | `.ace/scripts/llc_harness.py:751-770` (consistency-check in `pipeline_run`) | consistency-check is an advisory tooth — it surfaces drift after each step but never gates the pipeline. | **Working as designed.** `pipeline_run` runs `consistency-check.py` after each eligible step; output is printed but the result is **never tested** against the gate decision — no `if result.returncode != 0: block/return False`. A failing consistency-check cannot stop the pipeline or reject a gate. | P2 #15: `sed -n '751,775p' .ace/scripts/llc_harness.py` (see `docs/audit/p2-enforcement-log.md` #15). | none — working as designed; advisory only. | info (not a defect) |
| M-02 | LOW | enforcement-defect (pre-existing) | `.ace/scripts/llc_wave.py:_post_wave_check` (~line 524) | The `prp_verify` CRITICAL check runs regardless of whether `pre-wave-check.sh` exists — a missing baseline script should not silence CRITICAL pendências. | If `PRE_WAVE_CHECK_SCRIPT` does not exist, `_post_wave_check` returns `True` early (line ~524), which also skips the `prp_verify` CRITICAL block (the only `return False` path) — a CRITICAL `prp_verify` result is silently swallowed. | Final whole-branch review (M-2): the A-02 de-nest preserved this control flow verbatim (pre-existing, not introduced). Production is unaffected (the script exists in this repo); it only bites projects lacking `pre-wave-check.sh`. `BEHAVIOR_BASELINE.md` #6 already records the intended "False on prp_verify CRITICAL" contract. | sub-project-2 — decouple the `prp_verify` CRITICAL check from the `pre-wave-check.sh` existence guard so a missing baseline script cannot suppress a CRITICAL block. | open |
